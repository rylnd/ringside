# ringside

Determines fit and positioning of a rectangle within inner and outer bounds.

## Development

```bash
# install packages
npm install

# run the storybook server
npm run storybook

# run tests
npm test
```
